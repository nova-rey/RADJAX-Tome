# T4 Golden 1K Capture Pending

This directory intentionally contains no fabricated Golden 1K contract. M2A
implements the offline capture, validation, and semantic comparison machinery;
M2B must export the authoritative terminal T4 artifact and commit only the
compact reviewable contract records. See `docs/GOLDEN_1K_CAPTURE.md`.
