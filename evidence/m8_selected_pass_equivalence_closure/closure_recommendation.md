# M8 equivalence closure

The prior `logical_body_root` mismatch is a benchmark measurement-root error: the harness hashed selected-exemplar output file bytes, paths, and runtime JSON rather than a canonical logical-evidence projection. Ephemeral Modal output trees were not preserved, so a retrospective 71-body field comparison is unavailable.

Preserved reports show 64 sources, 71 coordinates, one selected-source pass, zero score-pass/C1-C5 execution, and no OOM or fallback for all sizes. Static tracing confirms model attention masks remain present; compact mask omission is only a persistence-format property. No concrete production batching defect was proven, so no inference correction or GPU rerun is justified.

The quantitative gate remains a miss: batch 8 median was 2.983 sources/s versus 2.780 for batch 1, a 7.277% gain. Keep batch size 1 canonical and close M8 with the explicit evidence limitation above.
