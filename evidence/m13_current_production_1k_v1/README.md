# M13_CURRENT_PRODUCTION_1K_REFERENCE_V1 compact contract

This compact v2 Golden contract is a checked-in projection of the retained
current-production reference. It is distinct from `tests/fixtures/golden_t4_1k`.
Use the explicit `golden validate --fixture` and `golden compare --fixture
... --artifact ...` commands documented in `docs/M13_CURRENT_REFERENCE_V1.md`.
