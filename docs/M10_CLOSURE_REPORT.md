# M10 closure report

Audited implementation commit: `cfa382b3af0a6d9210ea0cb42a0c8ad17f3946c2`. Contract remains pinned to `373e3d17060d4ce1c4a0db6065c9289da714bde7`; the Contract repository and bytes are unchanged.

The final M10 remediation closes the reproduced corpus-v2 tokenizer-boundary defect. Production preflight resolves the tokenizer exactly as canonical production: an explicit `tokenizer_id` wins, the fake-tokenizer sentinel and an omitted ID use the production `teacher_model` fallback, and HF/local IDs are constructed with the existing HF adapter in local-files-only mode. Preflight captures the authoritative binding through the existing tokenizer/binding APIs and compares canonical binding digests. A genuine mismatch fails closed with `CORPUS_TOKENIZER_BINDING_MISMATCH` before model or backend execution. Matching smoke bindings, fallback resolution, semantic-identity rejection, v1 compatibility, v2 loading, and v2 receipt-schema reporting have focused regressions.

Validation: focused M10/config/preflight/production suites pass (37 targeted audit/config/corpus tests; 44 production-stage tests). Full Python 3.11 suite passes: 1,224 passed, 25 skipped, 0 failed. Ruff check and format, compileall, diff check, wheel build, Contract-pinned clean-install CLI import/help smoke, and public build/validate help smoke pass. Python 3.12 is not installed on the Linux validation host and is recorded as unavailable. The inherited 50K bounded-memory evidence remains unchanged.

One focused read-only review passed with a reservation that its independent environment could not re-collect pytest dependencies; the authoritative venv receipts above are preserved. No GPU, teacher execution, or 50K rerun was needed for this preflight-only correction. Historical M8 evidence, Golden evidence, Student, and Contract remain untouched. This branch is ready for integration.
