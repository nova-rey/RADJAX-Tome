# Corpus Splits

The scaffold provides deterministic toy train, calibration, and final-test split
materialization. RADJAX-Contract validates the split for overlap by example,
token sequence hash, and source text hash.

