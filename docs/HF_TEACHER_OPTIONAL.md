# HF Teacher Optional Dependencies

Hugging Face teacher support belongs in RADJAX-Tome, but is not part of the
default scaffold. Future HF support must live behind the `teacher-hf` extra and
must not be required by default CI.

