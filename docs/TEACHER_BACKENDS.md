# Teacher Backends

The scaffold includes `FakeTeacherBackend`, which emits deterministic toy logits.

Real teacher backends will be added behind optional extras. Default tests must
not download models or require torch/transformers.

