# M8F completion: canonical reserve and cap wiring

Canonical C6 C2 construction now retains a complete deterministically ranked
candidate pool (`retain_complete_candidate_pool=true`) rather than treating the
micro-pool cap as evidence of eligibility exhaustion. Existing compatibility
callers retain the bounded default. The exact rational full-width pair is part
of SelectionIntent, resolved configuration, authority projection, C2 policy,
and C4 claim policy. C2 records preserve full-width classification when
effective top-K and vocabulary size are present; ordinary callers without that
metadata remain non-full-width by explicit default.

The legacy selector collapses repeated selected coordinates before assignment.
Canonical C2/C4 still preserves coordinate identity and multi-role obligations.
This checkpoint does not implement compact payload storage or body/manifest
transactions.
