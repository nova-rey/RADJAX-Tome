# B4 native-v3 Student v6 ordinary-production fixture

`tests/fixtures/native_v3_student_v6_smoke` is produced by the canonical CPU
smoke production builder and transactional explicit-v6 package writer. It
includes directory and `.tgz` forms plus the native M7 sibling used as the
selected-exemplar authority. The machine-readable receipt records separate
raw, package, composition, behavioral-authority, behavioral-source, language,
and resource identities. It makes no Student-training, accelerator, quality,
or default-profile-migration claim.
