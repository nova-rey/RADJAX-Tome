# Import Boundaries

RADJAX-Tome may depend on RADJAX-Contract.

Forbidden imports:

- radjax_student

Teacher-heavy dependencies such as torch and transformers are allowed only behind
optional extras and are not used in default tests.

